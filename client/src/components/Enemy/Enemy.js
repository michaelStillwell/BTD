// Import React
import React, { Component } from 'react';

export default class Enemy {
    constructor(hp, color, speed) {
        this.hp = hp;
        this.color = color;
        this.speed = speed;
    }
}